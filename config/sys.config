[
  {kernel,
        [{logger,
            [{handler, default, logger_std_h,  
                #{config => #{file => "log/websocket.log"}}}  
            ]}]},
  {websocket,
        [{port, 8080},
        {message_history_length, 50},
        {ip, {127,0,0,1}}]
    }
].
